📌 Estado Maestro del Proyecto — StudyForge (Resumen extendido)
🎯 Objetivo general
StudyForge es una aplicación web para gestión y apoyo en estudios, que integrará componentes de IA para enriquecer el trabajo con documentos (por ejemplo: extracción de información clave, búsqueda inteligente, resúmenes, preguntas generadas automáticamente).
El proyecto debe ser innovador, factible en 3 semanas, y demostrar un uso claro de IA integrado en una aplicación real.
________________________________________
🛠️ Stack Tecnológico
Backend
•	Lenguaje: Python 3.11.x
•	Framework: FastAPI (ligero, moderno, soporte automático de validación + docs).
•	Servidor: Uvicorn (ASGI).
•	Base de datos: PostgreSQL 15 (aún pendiente integrar).
•	ORM: SQLAlchemy (planificado).
•	Migraciones: Alembic (planificado).
•	Validación: Pydantic v2.11.
Frontend
•	Lenguaje: JavaScript/TypeScript (decisión final pendiente).
•	Framework: React.
•	Estilos: TailwindCSS (planificado, ya discutido).
•	Gestión de paquetes: Node.js LTS (22.x).
Infraestructura
•	Control de versiones: Git + GitHub.
•	Ramas activas: main, feat/walking-skeleton.
•	Colaboración:
o	Repo StudyForge → código fuente.
o	Repo Capstone → documentación, diagramas, informes académicos.
IA (por integrar)
•	Carpeta reservada en backend/app/ai/.
•	Subcarpetas preparadas: pipelines, models, prompts, vectorstore.
•	Plan:
o	Procesamiento de documentos en base a embeddings.
o	Generación de resúmenes y preguntas.
o	Posible uso de un modelo externo (OpenAI API u otro).
________________________________________
📂 Estructura actual del código
StudyForge/
  backend/
    app/
      __init__.py
      main.py
      routers/
        __init__.py
        health.py
        documents.py
      schemas/
        __init__.py
        document_schemas.py
      services/
        __init__.py
        document_service.py
      repositories/
        __init__.py
        document_repo.py
      ai/
        __init__.py
        pipelines/__init__.py
        models/__init__.py
        prompts/__init__.py
        vectorstore/__init__.py
    requirements.txt
  frontend/          (pendiente iniciar)
  docs/              (podemos usarlo para progreso técnico interno)
  .vscode/settings.json  → usa `backend/.venv` como intérprete
  README.md
________________________________________
✅ Lo implementado hasta ahora
1. Entorno
•	Python 3.11.x configurado en venv (backend/.venv).
•	Dependencias instaladas: fastapi, uvicorn, pydantic.
•	VS Code configurado para usar siempre el intérprete del venv.
2. Git/GitHub
•	Configuración SSH lista.
•	Remote origin → StudyForge.
•	Remote upstream → repositorio original del grupo.
•	Rama activa: feat/walking-skeleton.
3. Commits importantes
•	feat(api): add FastAPI health endpoint
•	refactor(api): move /health to APIRouter and include in main
•	chore(api): add base app package structure
•	feat(documents): scaffold routers/schemas/service/repo (in-memory)
4. Backend funcional
•	/health → endpoint básico de estado.
•	/documents (in-memory):
o	GET /documents → devuelve lista de documentos.
o	POST /documents → recibe title y content (y opcional description), crea documento con ID autoincremental.
o	Valida automáticamente entradas y devuelve errores claros (422 ValidationError).
•	Swagger UI (/docs) funcionando con modelos generados:
o	DocumentIn, DocumentOut, DocumentListOut.
o	HTTPValidationError (generado por FastAPI).
5. Arquitectura en capas (implementada)
•	Routers: definen endpoints y agrupan por dominio.
•	Services: lógica de negocio (intermediario entre router y repositorio).
•	Repositories: acceso a datos (ahora en memoria, luego PostgreSQL).
•	Schemas: validación de entrada/salida con Pydantic.
•	AI (stub): carpeta lista para integrar IA en el futuro.
________________________________________
⏭️ Próximos pasos
1.	Persistencia con PostgreSQL
o	Instalar sqlalchemy y psycopg[binary].
o	Crear archivo db.py para conexión.
o	Definir modelo Document en SQLAlchemy.
o	Migrar DocumentRepo a Postgres.
2.	Migraciones
o	Configurar Alembic para poder evolucionar la base de datos.
3.	Frontend mínimo
o	Crear app React en frontend/.
o	Conectar con endpoints del backend.
4.	Integración IA
o	Procesamiento de documentos (embeddings).
o	Funciones de resumen, búsqueda y generación de preguntas.
o	APIs expuestas desde /ai/.
________________________________________
💡 Notas adicionales importantes
•	Capstone repo: se mantiene para documentación, no para código.
•	StudyForge repo: toda la lógica y desarrollo van aquí.
•	Swagger UI: será una herramienta vital para probar el backend sin necesidad del frontend mientras se construye.
•	Entorno virtual: siempre debe estar activado antes de correr o instalar dependencias (.\.venv\Scripts\Activate.ps1).
•	Commits: estamos cuidando los mensajes (convención feat, chore, refactor) → esto hace el historial más claro y profesional.
•	Arquitectura: esta separación temprana (routers/services/repositories/schemas) nos asegura que cuando llegue la IA y la DB, no tengamos que reestructurar el proyecto.
